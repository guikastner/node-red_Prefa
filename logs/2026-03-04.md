# 2026-03-04

## Iteration 1
- Reviewed `Agents.md` and implemented MinIO logic via OpenTofu + script (no docker-compose).
- Added MinIO variables in `variables.tf`:
  - `minio_endpoint`
  - `minio_access_key`
  - `minio_secret_key`
  - `minio_bucket_name`
  - `minio_bucket_folders`
  - `minio_mc_image`
- Added `minio.tf` with `null_resource.minio_bucket_setup` to execute scripted bucket/folder creation.
- Added executable script `scripts/minio_setup.sh`:
  - Creates bucket idempotently.
  - Creates first-level folder prefixes using `.keep` objects.
  - Uses Dockerized `minio/mc` client.
- Updated `terraform.tfvars` and `terraform.tfvars.example` with MinIO settings.
- Updated README in English with MinIO component, variables and structure notes.
- Ran validations:
  - `tofu fmt -recursive`
  - `tofu validate` (success)
  - `tofu plan -refresh=false` (shows MinIO resource creation)

## Iteration 2
- Adjusted MinIO provisioning to avoid any auxiliary container usage.
- Removed `minio_mc_image` variable and related tfvars entries.
- Updated `minio.tf` to stop passing `MINIO_MC_IMAGE` and trigger field.
- Reworked `scripts/minio_setup.sh` to use local host `mc` binary directly.
- Added host prerequisite note for `mc` in README.
- Revalidated configuration with `tofu fmt -recursive` and `tofu validate` (success).

## Iteration 3
- Added explicit README topics for MongoDB and Backup, aligned with `Agents.md` requirements.
- Documented that MinIO is external (no MinIO container in this stack).
- Clarified that this repo provisions backup structure and bucket/folders, while backup execution (e.g., mongodump scheduling) is external.
- Updated `terraform.tfvars.example` to include backup-oriented folders: `backup`, `mongo-backup`, `output`.
- Ran `tofu validate` successfully.

## Iteration 4
- Implemented active backup process for MongoDB and Node-RED flows.
- Added backup variables in `variables.tf` (`backup_enabled`, schedule, retention, MinIO prefixes).
- Added `backup.tf` with:
  - backup directories creation,
  - secure generated backup runner (`local_sensitive_file`),
  - cron install/remove resources via `local-exec`.
- Added `scripts/backup_run.sh`:
  - `mongodump` archive from running Mongo container,
  - Node-RED flow archive from data directory,
  - upload of both files to MinIO via local `mc`,
  - local retention cleanup.
- Updated `terraform.tfvars` and `terraform.tfvars.example` with backup settings and MinIO folders.
- Updated README to document MongoDB backup mechanism and operational prerequisites.
- Ran `tofu fmt -recursive`, `tofu validate`, and `tofu plan -refresh=false` successfully.
